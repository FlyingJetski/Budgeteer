<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".ui.main.SourcesFragment">
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <Button
            android:id="@+id/viewExpensesButton"
            android:layout_width="180dp"
            android:layout_height="180dp"
            android:layout_marginStart="16dp"
            android:layout_marginTop="64dp"
            android:text="View Expenses"
            app:cornerRadius="20dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <Button
            android:id="@+id/viewExpenseCategoriesButton"
            android:layout_width="180dp"
            android:layout_height="180dp"
            android:layout_marginTop="24dp"
            android:text="View Expense Categories"
            app:cornerRadius="20dp"
            app:layout_constraintStart_toStartOf="@+id/viewExpensesButton"
            app:layout_constraintTop_toBottomOf="@+id/viewExpensesButton" />

        <Button
            android:id="@+id/viewIncomeCategoriesButton"
            android:layout_width="180dp"
            android:layout_height="180dp"
            android:layout_marginTop="24dp"
            android:text="View Income Categories"
            app:cornerRadius="20dp"
            app:layout_constraintEnd_toEndOf="@+id/viewIncomesButton"
            app:layout_constraintTop_toBottomOf="@+id/viewIncomesButton" />

        <Button
            android:id="@+id/viewIncomesButton"
            android:layout_width="180dp"
            android:layout_height="180dp"
            android:layout_marginTop="64dp"
            android:layout_marginEnd="16dp"
            android:text="View Incomes"
            app:cornerRadius="20dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>